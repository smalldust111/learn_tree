spring:
  application:
    name: template_springboot
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.12:3306/template?serverTimezone=CTT&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: root
  elasticsearch:
    rest:
      uris: 192.168.1.12:9200

server:
  port: 8080
  servlet:
    context-path: /api/template

logging:
  level:
    root: info
    com:
      sunyj:
        template:
          mapper: debug
